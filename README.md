# Book and Movie Server

run with;

    cargo run --bin book_server
    cargo run --bin movie_server
    cargo run --bin server_coordinator
    cargo run --bin client

build with;

    cargo build --release
